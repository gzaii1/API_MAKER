import { createStyles } from '@material-ui/core/styles';


export const MiddleStyles = createStyles({
    root:{
      width:'100vw',
      height: 'calc(100vh - 3rem - 1.5rem - 1px)',
      display:'flex'
    }
  })